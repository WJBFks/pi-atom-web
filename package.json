{
  "name": "pi-atom-web",
  "version": "0.2.49",
  "description": "当前 pi TUI 会话的 Web 界面",
  "type": "module",
  "pi": {
    "extensions": [
      "./extensions/index.ts"
    ]
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check extensions/index.ts && node --check extensions/server.ts && node --check extensions/dialogs.ts && node --check extensions/ask-user.ts && node --check web/app.js && node --check web/ask-user.js"
  },
  "dependencies": {
    "@fontsource-variable/noto-sans-mono": "5.2.8",
    "@highlightjs/cdn-assets": "11.12.0",
    "@xterm/xterm": "^6.0.0",
    "dompurify": "^3.3.0",
    "marked": "^17.0.0"
  }
}
