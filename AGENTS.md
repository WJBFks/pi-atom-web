# pi-atom-web 项目约定

- 前端已采用 Vue 3 runtime + Pinia + Vue Router memory history，入口为 `web/main.js`；普通 JS `h()`/Composition API 组件位于 `web/components/`，禁止恢复单体 DOM 渲染、SFC、构建工具或外部 CDN。`web/icons.js` 提供统一 SVG VNode，`web/clipboard.js` 提供统一安全复制。
- `shared/protocol.js` 同时供扩展和浏览器校验；快照与事件使用 `schemaVersion:1`，事件包含 `streamId/sequence/sessionId`。首次/切会发送 snapshot，其余发送 patch；live-only 更新不得遍历、复制或发送历史。**历史分页**：快照、显式 `messages` patch 和 `historyDirty` 延迟刷新都必须在出口统一由 `extensions/history-window.ts` 裁成「最近 `HISTORY_TURNS`（10）轮」的窗口 + `historyComplete`，用户提交 prompt 时不得借刷新路径发送完整历史；更早的历史由客户端用 `more_history`（带 `cursor` = 它当前最老一条的 id）逆序向前翻页，服务端用 `prependMessages` 推送，每页 5 轮。客户端只能对历史做**并集**（`mergeHistory`）：首个快照里 10 轮的窗口会随新轮向前滑动，若用替换语义就会在对话中间缺一轮；客户端仅在用户滚动或向上滚轮到消息区顶部时取一页，禁止连接后自动补完整段历史，并用补载前后的 `scrollHeight` 差保留可见位置。历史 ID 取 branch entry ID，完成消息的 `liveId` 用于迁移 Thinking 手动状态与计时。
- `web/api/` 负责单连接、鉴权、序号检查和取消；`web/stores/` 分离 session/conversation/composer/dialogs。组件拥有 DOM、观察器、计时器与终端实例，卸载必须清理。
- 验证包含 `npm run check`、`npm test`、独立 `npm run test:browser`；check 递归检查源文件，Node 测试与浏览器测试分开，禁止零测试假成功。浏览器测试使用内存 fixture 与本地服务器，不调用真实模型或写真实会话。

- `.agents/skills/`：Codex 项目级技能目录，当前包含 `pi-vue-nobuild`、`pi-package-compatibility`、`frontend-design`、`vue-best-practices`、`pinia`、`vue-router-best-practices`、`node`、`web-design-guidelines`、`accessibility-compliance`、`responsive-design`、`interaction-design`。这些技能供 Codex 在本项目中自动发现；不得放入供 pi agent 使用的 `.pi/skills/`。其中 `pi-vue-nobuild` 是浏览器架构约束，`pi-package-compatibility` 约束 `extensions/packages/` 与 `web/packages/` 的第三方兼容边界。
- 产品：pi-atom 系列的当前 TUI 会话 Web 扩展，命令 `/web` 与 `/web-wlan`；严格一对一绑定宿主进程当前会话。
- 不创建独立 agent、项目管理或会话管理系统。使用扩展 API 收发消息与订阅事件。
- `extensions/index.ts`：入口、`/web`、`/web-wlan` 与 `/web stop`、消息桥接、生命周期；通过 `pi.getCommands()` 提供命令列表，并补充 Web 命令 `/reload`、`/model`、`/session`、`/copy`、`/name`、`/compact`。`/reload` 分发到隐藏内部命令 `pi-atom-web-reload`，只在命令 handler 收到的 `ExtensionCommandContext` 上调用 `reload()`；严禁在会被事件替换的普通 `ExtensionContext` 上调用。`/model`、`/session`、`/copy` 在浏览器本地复用现有 UI；`/name` 使用 `pi.setSessionName()`；`/compact` 使用 `context.compact()`。进程内 handoff 复用端口/凭证并在新实例的 `session_start` 恢复服务。其余已知命令使用 `expandPromptTemplates: true` 分发，未知命令不得误发模型。
- `extensions/server.ts`：默认仅 loopback HTTP 服务、随机凭证、Host/Origin 校验、SSE、静态资源；静态表在启动时扫描、之后新增的前端文件不在表里，请求会落到凭证检查返回 401（浏览器取 ES 模块不带 `Authorization`），所以表未命中时用 `resolveWebAsset()` **按需读取** `web/` 下的文件 —— 边界保持严格：`resolve` + `realpath` 双重前缀校验（挡住 `..`、绝对路径与指向外部的符号链接）+ 扩展名白名单（`.js/.css/.html/.json/.svg/图片/.woff2/.ico`；`.ts`/`.map`/`.env` 不放行）；仅 `/reload` 进程内交接可指定旧端口与凭证。`/web-wlan` 用 `connection.host === "0.0.0.0"` 让服务监听所有网卡供局域网访问：URL 取第一个非内部网卡地址（跳过 169.254/16 链路本地与 198.18/15、198.19/15 代理 fake-IP 段，实体网卡排在 virtual/vmware/vEthernet/WSL/tun 等虚拟网卡之前）、其余网卡一并列出且**每张都带凭证完整地址**，`host` 随 `connection` 交给 `/reload` 交接；该模式**不校验 Host/Origin**（凭证是唯一凭据），已有服务时就地重绑并复用端口与凭证；TUI 会把每张网卡都列成带凭证的完整地址（凭证在 URL 的 `#` 片段里，只输 IP:端口会被网页提示缺少凭证），局域网模式的安全加固见文末「待办」。
- `web/`：页面、样式、浏览器交互（含 `/` 前缀筛选、键盘/点击补全）；Markdown 必须清洗；不能把不可信内容直接作为 HTML。
- Markdown 围栏代码统一由 `codeBlock()` 渲染：highlight.js 高亮、语言、总行数、独立行号 gutter 和复制按钮；正文及行号不得自动换行，复制只能读取 `.code-source code`，不能包含 gutter。工具 Input 必须复用该组件并显式使用 JSON。行内代码必须由 `.body :not(pre)>code` 提供与代码块同一套设计语言的标记：`--panel` 底、`--border` 描边、5px 圆角、`1px 5px` 内边距、`overflow-wrap:anywhere` 与 `box-decoration-break:clone`（全站基础字体已是等宽字体栈，只靠字体无法与正文区分）；围栏代码块内的 `code` 不得再套这层外框。highlight.js 使用 `@highlightjs/cdn-assets` 的本地浏览器构建，由 server.ts 暴露，禁止连接外部 CDN。
- 全页面及 xterm 统一使用 `--font-mono` 对应的字体栈：Noto Sans Mono、JetBrains Mono、Fira Code、Consolas、ui-monospace、Microsoft YaHei、monospace；局部组件不得覆盖成其他字体栈。Noto Sans Mono 使用 `@fontsource-variable/noto-sans-mono` 的本地 WOFF2，由 `web/fonts.css` 声明、`server.ts` 提供；禁止连接外部字体服务。
- 用户消息与命令消息使用 `.message.user` 右对齐内容自适应气泡；保持 `--user` 配色，短内容收缩，长内容受最大宽度限制后换行。用户气泡与助手消息都不显示角色标签，命令气泡仅显示“命令”，分支/上下文摘要显示各自的摘要标签。
- 浏览器消息区分为 `#message-history` 与 `#message-live`；流式消息/工具更新只能替换 live 区，历史内容变化时才更新 history 区，禁止每个增量重建完整 transcript。顶层条目（消息、孤儿工具卡、通知、自定义 Entry）与消息内部必须使用同一个 8px 间距：`#message-history>*{margin-bottom:8px}`、`.message{margin:0 0 8px}`，消息内 `.body details{margin:8px 0}` 与之一致；不得再写回全局 `details{margin:10px 0}`，也不得再出现 10px / 27px 之类的第二套节奏。
- 通用折叠块左侧类型图标与标题必须在同一行垂直居中；折叠态 hover 将类型图标切换为紧凑的实心向右三角，展开后显示实心向下三角。标题与预览之间的 `·` 必须是独立 `.disclosure-separator`，仅折叠态显示，展开态隐藏。
- 历史按“轮”折叠中间过程：一段连续的 assistant/toolResult 消息构成一轮的分组（`TurnGroup` + `turnGroups.js`），标题按固定顺序拼接「x 次思考过程 · y 次工具调用 · z 次技能调用 · a 条消息」（a = 折叠区内的正文段落数），数量为 0 的分段省略，没有任何中间过程时完全不渲染标题。本轮最后一条不含工具调用的 assistant 消息里，从最后一个非空正文块开始的部分是最终输出，留在折叠块之外；其余（含该消息里更早的思考）进折叠块，且折叠区里保留原下标以保证 Thinking 的稳定 identifier 不变。折叠块复用现有消息与工具卡渲染，自身去掉卡片外框：标题行左上角是原生 details 三角（收起/展开自动切换），标题下有一条分割线；不提供右侧 chevron、左侧灰线与子级缩进，正文与标题左对齐。仍在输出的一轮默认展开，**折叠时机是下一个用户输入开始**（不再是整轮输出一停就折叠；以会话 `busy` 为准的旧规则已废弃），用户手动操作过则以记录状态为准（一旦手动折叠/展开，后续用户输入也不再自动折叠）；**加载（首次快照 / `/reload`）时已加载的中间过程全部折叠**（`collapseLoaded` → `groupMessages(..., {collapseTail:true})`；会话正在生成时保留正在跑的那一轮），折叠后首屏节点数约降一个量级；折叠区沿用页面正文排版（`.turn-group{font-size:inherit}` 覆盖通用 `details` 的 12px），因此中间消息与最终输出样式完全一致；用户、命令、通知、自定义 Entry、分支/上下文摘要与终端输出各自独立渲染并切断分组；执行轨迹视图不做按轮折叠。
- Web 提交普通 prompt 时必须立即清空输入框；明确的 action 失败且用户尚未输入新内容时恢复原文字与图片。会话空闲时，浏览器在同一交互帧插入临时用户消息，后端同步发布可见的内存临时消息，真实 branch 用户消息到达后按会话、锚点、内容和提交顺序无重复替换；会话正在运行时，followUp 只显示在 Prompt 队列中，浏览器和后端都不得把它提前显示成主会话用户卡，直到 Pi 真正发出该轮的用户 `message_start`。后端 action 返回 `delivery: "started" | "queued"`，用于修正浏览器忙碌状态短暂滞后造成的乐观卡片。不得用固定超时把尚未出现 `message_start` 的正常慢 preflight 误判为失败。提示词模板与技能命令复用同一轮次等待绑定，但继续显示为命令记录。等待模型响应超过 1 秒才显示 `正在等待模型响应... (Ns)`，assistant 首个 start/update、agent 结束、取消或会话切换时清除；发送失败的轮次结束时必须清理尚未入历史的临时消息。临时消息和等待状态不得写入会话或发送模型。
- Prompt 队列兼容层位于 `extensions/prompt-queue.ts`：通过带 `Symbol.for()` 防重复包装的 `AgentSession._bindExtensionCore()` 观察器关联当前 session，监听系统 `queue_update` 并向 Web 发布 `promptQueue`（revision、总数、steering/follow-up 明细）。普通 Composer 固定以 followUp 排队；队列活动组件不提供额外输入区，只管理已提交的消息：steer（引导当前轮）与 followUp（排队后续轮）按上下顺序排列，各自标题、数量和说明合并为单行，数量为 0 的分组不渲染；用 `queue_update_item` 编辑文本或在两类间转换，用 `queue_remove` 删除；总数为 0 时整个活动组件不显示。后端仍保留 `queue_add` 协议能力。更新和删除必须携带最后看到的 revision 和派生 item id，服务端校验后以“清空并按原顺序重建”模拟操作；更新响应返回完整 `previous`、`updated` 和最终 `queue`，删除响应返回完整 `removed` 和最终 `queue`。revision 不一致时拒绝且不得改变队列；重建期间不得把中间空队列 patch 发给 Web。系统 API、扩展 API、RPC 和限制统一记录在 `docs/pi-prompt-queue-api.md`。
- Web 普通 Prompt 不得直接依赖 `pi.sendUserMessage()` 的返回时机：该正式扩展 API 是 fire-and-forget，可能在 `AgentSession` 完成 preflight、置为运行中之前就让 HTTP action 返回，连续提交会让两个 prompt 同时进入底层 agent。当前通过 `promptQueues.submit()` 调用捕获到的 `AgentSession.prompt(..., {streamingBehavior, preflightResult})`，只等“已启动或已排队”的 preflight 边界，不等待整轮模型完成；没有内部观察入口的旧运行时才回退正式扩展 API。
- 内容列宽度可由用户调整：hover `#messages` 左右边界时在鼠标高度显示细长手柄（`ColumnResizer`），拖动对称改变消息区、输入 dock 与状态栏共用的 `--content-width`，双击手柄恢复默认，键盘聚焦热区后 `←`/`→` 步进调整（`Shift` 加速）。手柄为 3px 细线、两端渐隐、圆头，拖动中改用强调色，不提供 hover 文字提示；热区宽 32px、只贴在文字区外侧以免遮挡文本选择，纵向从工具栏下沿一直延伸到视口底部，因此在输入框所在区域也能抓住边界；宽度上限按 `#scroll` 实际可用宽度（两侧各留 16px，滚动条出现时不溢出），下限 520px；宽度记忆在 `localStorage` 的 `atom-content-width`，宽度 ≤900px 的窄视口不提供手柄。拖动期间每个动画帧最多写一次宽度变量、读一次几何；内边距、滚动容器宽度、dock 顶边与手柄高度等环境读数只在挂载、尺寸变化与拖动结束时缓存，禁止每个 `pointermove` 触发多次强制同步布局。
- 禁止为历史条目开启 `content-visibility:auto`（含 `contain-intrinsic-size`）：被跳过的条目只渲染为“记住高度”的空白块，会在长会话里出现看不见的空白（用户实际遇到），且 scrollHeight 被估算值放大（实测 +11%～+63%）。该机制实测对拖动收益有限（真实内容 106ms/帧 → 关闭后 232ms/帧），不值得这些副作用。改变内容列宽度会重排可见区域的 transcript，优化方向应是减少每帧几何读取与拖动更新频率，而不是跳过屏外元素。
- `html/body` 禁止滚动；页面不渲染产品顶部标题栏和左侧会话栏，对话 `main` 占满整个视口，`.toolbar` 固定在顶部。`.toolbar` 分两行：`.toolbar-head` 是标题行（左侧 `#view-title` 显示会话名 + `#rename-session` 铅笔按钮，右侧 `#activity` 显示已同步/正在生成），下面一行是页面 tab（`ViewTabs`：对话 / 轨迹 / 上下文 / 设置，原生按钮 + `role="tablist"`，选中项用强调色 + 2px 下划线压在工具栏底边上，←/→/Home/End 切换并同步焦点、跳过 `disabled` 的 tab）。轨迹 tab 目前带 `disabled`（执行轨迹将改为独立页面，完成前不可选中，`nextTab` 会跳过它，也不提供作为默认视图）。tab 切换写 `composer.view`，对话与轨迹共用同一个 `#view-panel-feed`，上下文与设置各自一个可滚动的 `.page-pane`。**上下文与设置页不显示输入 dock**（连同其下的状态栏）：dock 保持挂载（否则 `ResizeObserver` 失去观察目标），由 `.dock-host{display:contents}` + `.dock-host.is-hidden .compose-wrap{display:none}` 隐藏，`--compose-height` 随之归零、`.page` 底部留白回到 20px；对话/轨迹页才显示 dock。**切 tab 只能切 `visibility`，不得用 `display:none`**：消息区必须留在布局里（`.view-pane.is-inactive{visibility:hidden}`），页面作为绝对定位叠加层盖住它；一旦 `display:none`，切回对话时要重排整段 transcript（300 轮 / 1.3 万节点实测：设置→对话 107ms、上下文→对话 130ms，改成 visibility 后 41/48ms，两帧基线 21ms），且滚动位置与 `--scrollbar-width` 会跟着抖动。重命名复用 `/name` 命令：把 `/name <新名称>` 作为 `send` 动作发回后端（必须带 `sessionId`，否则协议校验拒绝），Enter 提交、Escape 取消。`.toolbar` 下方的 `#scroll` 占满剩余高度并只承载消息滚动。`.compose-wrap` 是固定在 main 底部的浮动 dock，包含问卷/通用扩展请求和输入框；四者使用同一个居中列（宽度由 `--content-width` 控制，默认 860px）及一致水平 padding。输入框不展示附件占位或发送模式控件，Web 提交固定使用 followUp；右侧仅以 `模型 · 思考等级` 文本显示状态。用 `ResizeObserver` 把 dock 实际高度写入 `--compose-height`，`#messages` 与 `.page` 底部留白必须随之变化。`.compose-wrap` 在 `main` 内居中，而消息列在 `#scroll` 内容盒内居中，两者参照物不同：必须用 content-box 观察 `#scroll` 把滚动条宽度写入 `--scrollbar-width`，并让 dock 用 `left: calc(50% - var(--scrollbar-width,0px)/2)` 左移半个滚动条，否则长会话出现滚动条后输入框与状态栏会整体右移半个滚动条宽度（实测 7.5px）。长通用请求滚动 `#plugin-requests`，ask_user_question 保持页头/页脚固定并只滚动 `.ask-content`。仅当 `#scroll` 位于最底部时自动跟随消息；用户向上滚动任意距离后立即停止，重新到达底部后恢复，关闭「自动跟随最新消息」后不再自动跟随。顶部不显示“最新消息”，离底时改在输入框上方居中显示圆形向下按钮；点击必须先同步恢复自动跟随，再在 DOM 更新和下一渲染帧校准到底部，避免流式 Thinking 增长超过一次性滚动。
- 页面偏好集中在 `web/stores/settings.js`：主题（`atom-theme`：light/dark/system，跟随系统时监听 `prefers-color-scheme` 并切换 `html.dark`）、内容列宽度（沿用 `atom-content-width`，设置页与拖动共用同一份记忆，双向用 `atom-content-width` 窗口事件同步）、默认视图与自动折叠/自动跟随（`atom-settings`）。设置页（`SettingsView`）是左侧分类导航 + 右侧内容（`SETTINGS_SECTIONS` 四类：外观 / 会话信息 / 连接与实例 / 行为，`nextSection()` 支持 ↑/↓/Home/End，垂直 `role="tablist"`，窄视口折叠为横向）；会话信息与状态栏弹层共用 `DetailRows`/`statusRows`，连接与实例只展示地址、实例 ID、连接状态与凭证是否已保存，**不得回显凭证本身**；“恢复默认设置”放在行为分类末尾。外观里含内容列宽度与安全区，两者都用同一种控件组合：左侧数值输入（回车/失焦提交）+ 滑块（`input` 实时生效）+ 最右侧统一用 `reset` 图标表示还原（已处于默认值则禁用）。内容列宽度上限取当前视口可用宽度（`maxContentWidth(availableWidth())`，窗口 resize 时重算）；安全区为顶部/底部两个 px 值（`clampSafeArea()`，上限 240），写成 `--safe-area-top/-bottom` 后由 `.layout` 的 padding 消费；**软键盘让位**：`watchKeyboard()` 监听 focusin/focusout，当「触屏设备（`(pointer: coarse)`）+ 焦点在 INPUT/TEXTAREA/contenteditable」时认为键盘占着底部，`appliedSafeAreaBottom` 归零（配置值与设置页显示不变，只是实际生效值归零；顶部安全区不受影响），失焦后自动恢复。——整个窗口内缩，工具栏不会钻到手机状态栏下、输入框不会被底部工具栏盖住；值与其他偏好一同落盘 `atom-settings`。上下文页（`ContextView`）展示上下文构成、Token 用量、缓存与成本，以及 `branchSummary`/`compactionSummary` 摘要条目（`contextSummaries()` 纯函数）。关闭自动折叠后 `TurnGroup` 始终展开（用户手动折叠仍优先），关闭自动跟随后 `ConversationFeed` 不再随新内容滚底。
- 输入框左侧图片按钮支持选择 PNG/JPEG/GIF/WebP，页面支持拖入图片且输入框支持粘贴图片，提交前显示可移除缩略图；最多 4 张、单张 8 MiB、合计 16 MiB，图片与可选文本作为同一条用户消息通过 `pi.sendUserMessage()` 发送。附件只在当前浏览器内存中保留至发送或移除，不写入 Web 派生状态。
- 含图片的用户消息使用右对齐附件条：图片以 80px 方形缩略图横向排列在文字气泡上方，文字继续使用独立的 `--user` 自适应气泡；纯图片消息不生成空文字气泡。
- 输入框附件和会话中的图片必须复用 `PreviewImage`：点击缩略图通过 Teleport 打开全屏等比例预览，提供 SVG 关闭按钮，并支持点击遮罩和 `Esc` 关闭；关闭后焦点返回原缩略图。
- thinking 内容使用 `.thinking-block` 专用 details：无外框无底色（随折叠块统一样式），summary 以固定加粗 `思考 ·` 开头；折叠摘要把 `\\s+` 压成单个空格，并以 DOM 实际 `clientWidth/scrollWidth` 二分截断为一个视觉行，仅溢出时追加三个半角点 `...`，resize 后重算；展开时隐藏摘要文本并按原始 Markdown/换行显示全文。流式 thinking 默认展开并计时，**折叠时机**：下一个思考块一开始，前一个就折叠；整段回话结束时（消息离开 live 区）最后一个也折叠（`lastThinkingIndex()`：只有最后一个思考块在生成时保持展开）；思考计时以“同一轮内思考块之后是否已出现正文或工具调用块”为结束界，后续输出时间不得计入，只有位于消息末尾、仍在生成的思考块继续计时并在整轮结束时结算；折叠耗时到秒且不足一秒显示 `<1s`、展开耗时到毫秒，耗时始终相对 summary 标题行垂直居中。以稳定消息标识记录用户在运行期间的手动 toggle，一旦操作过，后续流式重绘和完成转换必须保留用户最后状态，不得自动折叠。
- 工具调用按 `toolCallId` 合并 assistant 的 `toolCall` 输入和 `toolResult` 输出；运行中事件在同一卡片显示，并合并 update 与 start 以保留输入参数。所有折叠/展开块（思考、工具调用、通知、自定义 Entry）统一为：**无外框无底色、标题一律灰色、标题形如 `[图标] 类型 · 标题`**；**状态只有三种：正常=灰色、警告=橙色、报错=红色**：报错即工具调用失败，此时整行（类型图标、标题、单行摘要）一律标红；运行中、成功与技能卡都属于「正常」灰色，通知按级别映射（info=正常灰、warning=警告橙、error=报错红）；折叠态常态显示类型图标、悬停时换成实心向右三角，展开态常态是实心向下三角；中间过程分组（TurnGroup）维持原样。通用工具的正文直接分为 JSON Input 与 Markdown Output，不使用通用面板或 Output 内框；二者背景均为 `--bg`。Input JSON 必须保持 `white-space: pre`，禁止自动换行并允许横向滚动；Markdown Output 内的代码块不限制高度，不得出现纵向滚动条。read/write/edit/bash/grep/find/ls 等专用工具继续使用各自视图。状态规则同上（正常灰／警告橙／报错红）；右侧垂直居中显示执行耗时，折叠到秒且不足一秒显示 `<1s`，展开到毫秒，运行时由浏览器计时、完成后使用后端记录的最终值。工具块全部继承项目默认字体。读取 `SKILL.md` 的 `read` 调用按技能卡展示，判定必须与 pi 宿主一致（`dist/core/tools/renderers/read.js` 的 `getCompactReadClassification`）：仅 `read` 工具、路径 basename 为 `SKILL.md`、大小写敏感、`file_path` 优先于 `path`；折叠标题显示 `skill` 与技能目录名，配色沿用普通折叠块的正常灰（不再单独着色）。
- **pi 系统工具的专门适配写在核心代码里**（`web/components/conversation/ToolCallBlock.js`），**不放进 `web/packages/`**：`packages/` 只用于第三方 package 兼容。目前：`write` 正文直接展示写入内容（以 `file_path`/`path` 推断语言交给 `codeBlock()` 高亮，带语言、行数与复制），`edit` 正文的 `-`/`+` 变更**带文件行号**（结果里的 `details.diff` 是宿主算好的带行号格式 `+123 内容` / `-123 内容` / ` 123 内容`，与 `dist/modes/interactive/components/diff.js` 的 `parseDiffLine` 同格式，`parseToolDiff()` 解析后渲染）；结果还没回来时才回退到 `edits[]` / 单个 `oldText,newText`（兼容旧的 `old_string/new_string`）推算——**参数里没有行号，所以这种情况行号列留空**，不编造行号；**diff 必须整块横向滚动、行号固定**：`-`/`+`/行号放在同一个 `.diff-gutter` 里 `position:sticky;left:0`，且内容层 `.diff-scroll` 必须是 `width:max-content;min-width:100%`——sticky 只能在自己所在的“行盒”内吸附，行不铺满可滚动宽度时 gutter 会跟着滚走（实测滚动 1720px 后 gutter 从 112 跑到 -880）。`read` 的结果按 `file_path` 推断语言交给 `codeBlock(str, language, { startLine })` 高亮，**行号必须从参数里的 `offset` 开始**（不是 1）；`bash`/`powershell`/`grep`/`find`/`ls` 的结果是逐行文本（终端输出、`文件:行号: 内容`、路径列表），用 `pre.plain-output` 原样展示（可换行），**不得走 markdown**——`#`、`*`、`_`、`|` 会被改写成标题/列表/强调/表格（`isPlainOutputTool()` 维护这份名单）；标题主参数按工具取：grep/find 用 `pattern`（与宿主一致），read/write/edit 用 `file_path`，其余走 `toolTitleArgument` 的通用顺序；**工具自己的提示语**（`[246 more lines in file. Use offset=230 to continue.]`、`[Truncated: ...]`、`[Full output: ...]` 等）由 `splitToolNotices()` 从输出末尾抽出来，作为代码块/终端块之外的备注行展示（它们不是代码，不得包进代码块；只认宿主实际会写的那几种提示头，避免把正文里形如 `[link]` 的行误判）；**read/grep/find/ls 的 INPUT 用可读参数行**（`toolInputRows()`：read 路径/起始行/行数，grep 模式/路径/文件过滤/忽略大小写/上下文/上限，find 模式/路径/上限，ls 路径/上限），原始 JSON 收进同一个二级折叠区；`bash`/`powershell` 用对应语言的代码块展示命令；其余工具（参数形态不一）继续直接展示 JSON。文件内容与终端输出全部归 OUTPUT；`write`/`edit` 这种大体量入参才把内容/变更放到 Input 位置、原始 JSON 收进二级折叠；两者的原始 JSON 都收进「原始 Input」二级折叠区（不再直接占用 Input 框）。标题参数顺序为 `command → file_path → path → …`（宿主主用 `file_path`，因此 `read`/`write`/`edit` 标题能显示路径）。
- Thinking、工具调用、TUI 通知及自定义 Entry 必须复用 `DisclosureBlock` 的统一 `details/summary` 折叠结构；各业务组件只负责标题、状态、计时及正文。所有折叠摘要必须把连续空白和换行压成单个空格，只占一个视觉行并在溢出时显示省略号。折叠行必须保持紧凑：`summary{line-height:1.45}`，折叠块标题行 `padding:6px 0`（无卡片内边距、无底色），通用 `details:not(.turn-group){padding:0;border:0;background:transparent}`；不得再给折叠块加回边框/底色或大内边距。类型图标与三角共用 16px 的 `.disclosure-icon` 槽位（绝对定位叠放、只切 opacity），这样悬停换图标不会让标题位移。
- Pi branch 中所有 `type: "custom"` Entry 均须通用转发为 `customEntry`，不得按插件名特判；后端观察 Pi `ExtensionRunner.getEntryRenderer()` 得到实际注册的 TUI renderer，分别生成折叠态与展开态文本、清除 ANSI 控制码，并对 `customType`、`data` 和时间戳做有界 JSON 归一化。Web 通用卡片优先展示 renderer 文本，展开正文使用与 TUI 通知相同的默认字体，原始 JSON 收入二级折叠区；renderer 缺失或失败时，对象数据以 `[JSON OBJECT]` 为摘要并用 JSON 代码块展示，其他值按纯文本展示。浏览器不得执行 TUI 组件、插件 HTML 或其他不可信代码。
- `extensions/tool-events.ts` 只向 Web 协议暴露工具卡需要的字段，并将扩展自定义的 `args`、`partialResult` 转为有界 JSON；循环引用、`BigInt`、异常 getter 或私有句柄不得导致 SSE 断开。单条 SSE 事件无法编码时不得销毁连接或消耗序号。
- Node HTTP `response.write()` 返回 false 仅表示背压，数据已经接收，禁止据此删除或销毁 SSE 客户端；仅写入抛错或连接 close 时清理。
- SSE 协议必须校验 JSON 序列化后的实际传输形态；Pi 内存对象允许存在会被 JSON 忽略的 `undefined` 可选字段，不得因此拒绝整个快照或增量。
- 每次扩展加载生成新的 `instanceId` 并放入快照；Web `/reload` 保存旧实例 ID，SSE 连到不同实例后清除标记并执行一次 `location.reload()`，不得在旧实例仍存活时提前刷新或形成刷新循环。
- Web `/reload` 的旧 `instanceId` 必须在 POST 发出前写入 sessionStorage，避免新实例在响应返回前恢复而错过刷新；仅明确的命令错误清除标记，重载导致的 fetch 网络中断须保留标记等待新 SSE。
- `sessionStats()` 从当前 branch 汇总轮数、事件数及 message/toolResult/compaction/branch_summary usage，向输入框下方状态栏提供真实工作区、Token、缓存命中率与成本；输出速度标记为按会话活跃时长估算。状态分组点击显示详情，复制使用与代码块相同的安全剪贴板回退。
- 状态栏每秒刷新时只能更新已有的 `[data-status-value]` 文本节点，不得重建按钮 DOM，否则会中断 hover/focus。`.compose-wrap` 底部 padding 固定为 0，状态栏与输入框、dock 底边的对称垂直间距统一由 `.status-shell` 提供。
- 状态栏按钮固定使用 24px 高度，文字与 SVG 使用相同的 14px 行盒；按钮和文字用 flex 垂直居中，SVG 用 block 且不得保留基线偏移。
- 状态栏第一项显示 `pi.getSessionName()` 返回的会话标题，空标题统一显示“未命名会话”；点击后展示与左下角一致的“会话信息”，不得退回工作区摘要卡。标题行与状态栏会同时出现会话名，这是刻意保留：标题行用于重命名，状态栏第一项是会话详情的入口。
- 输入框右侧模型与思考级别均为无前置图标的独立可点击文本设置。模型清单优先使用 `context.scopedModels`，未设 scope 时使用 `context.modelRegistry.getAvailable()`；提交后必须按 provider+id 从当前列表重新定位模型，再调用 `pi.setModel()`。有当前模型时默认选中其供应方与模型；无当前模型时仅单供应方自动选中，多个供应方不得自动选。思考菜单默认选中 `context.thinkingLevel`，并按当前模型的 `reasoning` 与 `thinkingLevelMap` 仅展示受支持级别，通过 `pi.setThinkingLevel()` 更新且后端重复校验；供应方筛选在选择器打开期间保持用户选择，不得被状态重绘重置；“显示全部”按供应方分组并在各组前展示供应方名称；右侧标题与搜索框固定，仅其下方模型列表滚动。生成期间禁止切换二者。
- `#composer` 使用顶部略宽、其余方向紧凑的内边距：桌面端 `10px 12px 8px`、移动端 `9px 9px 7px`；`#prompt` 不得设置上下 padding，默认显示 2 行并随输入自动增高，最多显示 10 行且不得超过会话区可用高度；`.compose-actions` 最小高度为 34px，与 32px 发送按钮匹配。
- Web UI 操作图标统一使用 `web/icons.js` 的内联 SVG 图标组件，不得使用 Unicode/字体图标模拟；底部状态栏图标使用相同尺寸、描边及按钮高度。状态详情与左下角会话详情弹层必须互斥，只有触发按钮和弹层自身属于内部点击区域，点击其余任意位置均关闭。底部工作区摘要不显示会话短 ID。会话详情不重复会话名，依次展示会话 ID、会话文件路径、所在工作区、所在工作区路径、活跃时间、创建时间和活跃时长；可复制字段使用 SVG 复制按钮。
- 底部 Token 输入/输出必须使用独立但几何对称的 `up`/`down` SVG，保持相同画布、路径长度和描边；不得复用圆形发送按钮的 `send` 图标。发送按钮使用 32px 圆形和 15px 图标，以 grid 居中；hover/active 保持 accent 色系，不得继承通用按钮的浅灰背景。
- `test/`：HTTP/SSE 与扩展桥接集成测试，禁止调用真实模型或写真实会话。
- Web 派生会话状态落盘到工作区 `.pi/atom/<sessionId>/web-state.json`，包括 Pi 会话文件不保存的 Thinking/工具最终耗时、用户折叠状态以及命令/通知镜像；同目录临时文件原子替换并限制为 500 项。连接凭证、临时 Prompt、运行中状态、滚动位置不落盘。跨项目设置预留 `~/.pi/atom/`，未有全局设置时不得创建。
- 修改扩展后同步更新本文件、README.md 和 CHANGELOG.md；文档使用中文。
- 验证：`npm run check`、`npm test`。依赖由 package-lock.json 固定。`check` 递归检查 `extensions/`、`shared/`、`web/` 下的 JS/TS 语法，并额外拦截 CSS 的行首 `//` 注释（CSS 里非法，会静默吃掉下一条规则）。

- pi 扩展后端使用 `.ts` 文件：已复现当前加载器会缓存原生 ESM `.js`，即使重新加载仍调用旧工厂；不要改回 `.js`。浏览器继续使用 `shared/protocol.js`，后端必须使用内容同步的 `shared/protocol.ts`，避免 `/reload` 后继续引用旧协议校验器。

- **状态提示**（`CommandRule`，命令 / 模型切换 / 已中断三类共用）：渲染成 `-----文字-----`（左右两段线夹居中文字），线用 flex 行内实心块（`height:1px`）而不是 `position:absolute;top:50%` —— 后者在行高为奇数时落在半个设备像素上，1px 会被渲染成 2px，同一页里时粗时细。命令默认只显示命令名，圆圈感叹号 hover 弹**自定义浮层**（不用系统 `title`，样式不可控且有延迟）：第一行全文、第二行操作提示，点击复制（反馈只体现在图标变对勾，不显示「已复制」文字）；行内的命令名/取值不加 `--panel` 底与描边，只有浮层里的全文那一行用面板框。模型与思考级别的切换**统一**显示为 `` 模型已切换为`模型 · 级别` ``（不单独为思考级别出提示），且**只提示最后一次**：`pendingStatusNote` 单值覆盖，不能用「值没变就不记」去重，否则 A→B→A 的最后一次会漏。中断提示统一为红色「已中断（：原因）」—— 用户中断与模型/工具报错**不再区分文案**，分割线也保持红色；中止产生的固定措辞（宿主 `Operation aborted`、Node `This operation was aborted`）已由该横线表达，消息正文与工具卡都不再重复显示，判定统一放在 `web/components/conversation/abort-notice.js`，用**整段精确匹配**而非「包含 aborted」（避免误吞正常输出）。
- 命令输入与 `ctx.ui.notify` 是独立展示记录，按会话和分支锚点合并到网页消息，最多 500 条，落盘到项目级 Web 会话状态且不送模型。服务启动时包装共享 UI notify；`/reload` 的替换实例必须在 `session_start` 第一次异步操作前恢复包装，并将状态加载期间收到的通知合并落盘，停止/退出时恢复原方法。本扩展连接地址通知必须绕过镜像，防止凭证泄漏。

- `extensions/dialogs.ts`：按 UI 方法统一桥接 select/confirm/input/custom，不按扩展命令特判；editor 仅展示并提示 TUI 完成。请求 ID + 会话校验，双端首次完成生效，停止恢复原方法；custom 使用同一个组件，镜像 render 的 ANSI SGR 样式与列宽，首次出现 custom 请求时才加载本地 xterm.js 与样式，按比例缩放并转发 handleInput；输入按顺序发送，更新保留终端实例和焦点。禁用 OSC 等非样式控制，不引入内层滚动条。

- 扩展请求面板位于 `.compose-wrap` 内、输入框正上方，与输入框同宽；仅外层面板限制高度并滚动，选项列表和 pre 不再独立滚动。
- **活动组件栏**（`web/components/composer/ActivityBar.js`）也在 `.compose-wrap` 内、输入框正上方（插件请求面板之后）：一排像浏览器标签页的活动组件 tab（有图标有名字，按 `priority` 降序、同优先级按 `arrivedAt` 升序排列），栏自身不画背景与边框，颜色全部来自每个 tab 自己的 `tone`（`{color,background,border}`，由组件内联下发，CSS 不写死色值）。tab 配色三态：**未激活 = 默认背景 + 默认文字 + 默认边框（`--border`）**；**未激活 hover = 默认背景 + 该 tag 的字体颜色**；**激活 = 该 tone 的背景/文字/边框**（hover 时底色向该 tone 主色微染）。边框**粗细**始终一致，只有颜色在激活时切换。**每个 tab 都保留完整的左右边框**（相邻两个之间就是一条 1px 线）；此前“去掉后一个的左边框 + 只有最后一个有右边框”的两条规则叠加后会使相邻处完全没有竖线。点击 tab 后**所有 tab 一起升到大框顶部的那一行**，输入卡上方的 tab 栏随之消失；收起时整排落回原位，**折叠只能点行尾的最小化按钮（或键盘 `Esc`）——展开后点击 tab 只切换、不折叠**；`focused` 与 `expanded` 是两个独立状态（方向键只移焦点、不展开），切换后需 `nextTick` 重新聚焦（节点会被重建）。大框：`margin-top:-1px;border:1px solid var(--border);border-top:0;border-radius:8px 8px 0 0;overflow:hidden`（上方两角圆角由外层提供、overflow 把 tab 行的直角裁进圆弧；下方两角直角，下沿贴着输入卡）；顶边由 tab 行画一条整宽上边框（`border-top:1px`，`margin:0 -1px` 外扩抵消面板左右边框，使首个 tab 的左边框与面板边框重合），**行内 tab 一律 `border-top:0`**（否则两条错开 1px 会顶边断裂），**整块大框（外框 + 内容区那一圈）与所有内部分割线（tab 行下边、头部底边、底栏顶边）一律使用中性 `--border`，且两个主题下都保持中性**；只有 tab 自身在激活时用 tone 配色。内容区那一圈与外框是同一条 1px 线（靠 `margin:0 -1px -1px` 重合），若给它上 tone 色，同一条线会在内容区开始处从灰变紫、看起来像两套边框；tab 行与内容区之间有一条分割线：**画在行上的绝对定位整宽线**（`.activity-tabs-in-panel::after`，`left:0;right:0;bottom:0`），一路铺到右边缘、含最小化按钮那段；**当前展示的 tab 用它自己的 `::after`（`background:var(--tone-bg)`）把那一段盖掉**，与大框内容区连成一体。不要把线做成各元素的下边框——逐块拼接时线只画到按钮左侧就断；tab 行 `align-items:stretch`，最小化按钮 `align-self:center` 并贴右缘，行内剩余空白用 `::before`（`order:1`）撑开。活动组件包括：每个未完成的**扩展请求**各占一个 tab（`priority` 统一 1000、按 `arrivedAt` 先来在左），带 `packageId` 的请求可用 `PACKAGE_ACTIVITY_LOOK` 定制 tab 外观（如 `@juicesharp/rpiv-ask-user-question` → 名字「提问」、`help` 图标、黄色 tone），其余沿用默认紫；**Prompt 队列**在生成中或队列非空时显示一个 tab（`priority` 500，显示实时总数），活动内新增消息默认进入 followUp 排队，队列项可在 followUp（排队后续轮）与 steer（引导当前轮）间转换，并支持就地编辑和删除，且 `autoOpen:false`，不得因普通生成自动展开。扩展请求大框内容直接渲染 `RequestView`（专用组件或通用 `GeneralRequest`）。请求带 `blocking` 字段（默认阻塞）：有阻塞时禁止提交 prompt 与发送按钮，并在活动栏**上方**显示红色提示行（`blockedMessageParts()` 返回分段、`blockedMessage()` 返回纯文本：1 个用名称、2 个用「A和B」、3 个及以上用「A、B等共N个活动」；名称取与 tab 一致的显示名，渲染时活动名包成**行内代码**，该行与其中的 `code` 都**不使用不透明背景**，只有一圈描边）。
- **活动组件是独立浮层，不影响会话布局与滚动**：`.dock-overlay`（`position:absolute;left:0;right:0;bottom:100%`，容器为 `.compose-wrap` 的 padding box）承载「阻塞提示 + 活动组件栏」，**绝对定位使其不占 dock 高度**，因此展开/折叠大框时 `--compose-height` 不变、`#messages` 的底部留白与滚动位置都不动（实测三种状态都是 155.39px）。`.compose-wrap` **不自铺背景**（旧的全宽渐变已删除），浮层本身透明，可以**透视下层滚动的消息**；输入卡 `#composer` 与状态栏各自有背景 —— `.status-shell` 用 `margin:0 calc(var(--dock-pad,28px)*-1)` + 同值 padding 铺满整宽的不透明底（`--dock-pad` 在窄视口改为 18px），保证「输入框及以下」始终有背景。
- **折叠只隐藏、不卸载**：非当前的大框放在 `.activity-panel-host.is-collapsed`（`height:0;overflow:hidden`）里保持挂载，否则内部表单的本地状态（问卷草稿、终端实例）会在折叠后丢失；tab 行只有**一条**（共享），不能每个面板各渲染一条，否则 `.activity-tab` 查询会数到隐藏面板里的重复 tab。
**排序**：`sortActivityTabs()` 按 `priority`（浮点，可正可负）**降序**排列（大的在左），优先级相等时按 `arrivedAt`（到达时间戳）**升序**（先来的在左）；纯函数、不改传入数组、缺字段按 0 处理。`tabs` 作为 props 传入（默认 `ACTIVITY_TABS` 演示数据），关闭状态只存内存不落盘。

- `extensions/packages/` 与 `web/packages/` 按 npm package 名组织 Pi 第三方兼容；后端 registry 只在观察到 package 的唯一公开事件后动态导入对应模块，Web registry 只在收到带 `packageId` 的实际请求后动态导入组件和本地 CSS。未安装、未触发或未知 package 不生效、不报错、不警告；标准 UI 请求继续走通用桥，未知 custom 使用终端兜底。package 载荷的**字段级 schema 由包模块自己校验**（后端 claim 前 + Web 渲染前），`shared/protocol` 只做通用有界校验（JSON 可序列化 + 总长度），不得出现 package 专属字段名。
- `extensions/packages/@juicesharp/rpiv-ask-user-question/`：适配 2.9.0 公开 prompt 事件与 QuestionnaireResult；工具参数和事件双重匹配，并识别 2.9.0 间接 `Session` 构造及旧版 `QuestionnaireSession` 工厂，直接构造包含答案、每题备注、preview、全局备注和取消状态的结果，不解析终端文本、不模拟按键。适配器自带宿主问卷 schema 上限（1–4 题、每题 2–4 选项、header ≤16、label ≤60），工具调用不合契约时不认领（静默回退通用终端）。结果构造对齐宿主 `state-reducer.ts`：未答与「既无勾选也无文本」的题不出现在 answers 中，多选答案是 `selected` 标签 + `answer:null`，无勾选时自由回答作为 `kind:"custom"` 答案（多选下自由文本作为额外 `selected` 项），notes/globalNote 仅非空时附加。
- `web/packages/@juicesharp/rpiv-ask-user-question/`：1–4 题结构化表单，覆盖单选、多选、多行自由回答、每题备注、单选 Markdown preview、多题 tab、核对、未答提示、部分提交、全局备注、取消、收起和 sessionStorage 草稿恢复。两端未提交草稿独立，最终先完成者生效。渲染前用包内 `sanitizeQuestions()` 把请求裁成有界、不会抛错的形状（字段级 schema 属于包模块，不在 `shared/protocol` 里）。

- ask_user_question 使用紧凑独立卡片，选项垂直排列；自由回答是同款末尾选项卡，只显示随题型变化的选择框（单选 radio、多选 checkbox）与内嵌文本框，不显示重复标题。单选 focus/input 选择 custom 并清除普通选项；多选的勾选（`options`）与自由文本（`text`）是**两个独立存储**（宿主 `multiSelectChecked`/`customDraftsByTab`）：勾选或取消勾选**都不清空**自由文本，输入文本**也不清空**已勾选项，两者可共存；取消自由文本行的勾选只取消选中态、**文本保留**。自由文本行自身的 `custom` 勾选态独立于其它选项，且**不能由 `text` 反推**：勾上但尚未输入时 `text` 为空，也必须是「已勾选」；因此 `choose()` 绝不写 `custom`，其余选项的勾选/取消也绝不影响它。「已作答」统一由 `isAnswered()` 判定（多选=勾选或文本任一非空），tab 上的 ✓、核对页未答提示与提交按钮共用。**`kind` 按题型固定**：多选题恒为 `multi`（自由文本由 `text` 携带，提交时并入 `selected`），单选题的自由回答为 `custom`；在单选问卷上写入 `kind:"multi"` 会被后端按多选校验并报错。自由回答说明与备注标题放入各自 placeholder。备注按需展开；清除与取消按钮放到底部操作栏右侧，和导航/提交集中排列；单题直接提交，多题保留核对。卡片随内容自然增高，最大为对话区域可用高度，内容过长仅内容区滚动，页头与操作栏不随其滚走。任何表单重建必须保存并恢复页面、对话、请求面板及 ask-content 的滚动位置，禁止交互后跳顶。
- 问卷顶栏必须显示当前问题的原始 `question.header`，包括单题问卷；题号/总数/题型作为 muted 次要信息，核对页显示“核对答案”，不得用固定文案覆盖 header。
- 单选 preview 的选择区与预览区使用 `.ask-option` 统一外框，选中边框/背景作用于整个组合，不允许上下块边框重叠或断裂；预览正文另有一个与 `.code-block` 同一套视觉语言的内层面板框（`--panel` 底 + `1px --border` 描边 + 8px 圆角 + 内边距），选项卡的选中色只作用在外框上。框内块间距统一为项目的 8px 节奏（浏览器对 `<p>` 的默认 `margin:1em` 在 12px 字号下是 12px，必须显式覆盖），且容器（引用/列表项）内部段落的外边距归零，否则容器会被撑成空壳。折叠条使用紧凑样式并紧贴对应选项；折叠时不继承通用 details 的大间距，展开后再为正文增加间距。展开项索引保存在问卷本地 `previews` Set，任何表单重建均须恢复。preview 是 Markdown，但契约（「多行文本按行渲染」）要求同时开启 GFM 硬换行（`MarkdownContent` 的 `breaks`）并让预览段落 `white-space:pre-wrap`：少了任一项，ASCII 布局的换行或行首缩进就会被折叠、列对不齐；`pre-wrap` 只能落在预览内部的**叶子块**上（`.ask-preview .body :is(p,li,td,th):not(:has(>p,>ul,>ol,>blockquote,>pre,>table))`）：容器元素一律保持 `normal`——写到 `.body` 会把块间空白变成多余空行，写到引用/松散列表项这类容器则会把容器内部的换行变成空行（实测引用被撑到 6 个空行），只盖 `>p` 又会漏掉列表/表格里的空白。预览正文字体与代码块使用同一份 `font:12px/1.65 var(--font-mono)`，保证行高、字号与字形和代码块一致。

## 待办

- **执行轨迹改为独立页面**：目前 tab 保持可见但 `disabled`（视图代码 `ConversationFeed` 的 `trace` 分支仍保留、无 UI 入口）；独立页面完成后接回去，并重新允许把它设为默认视图。
- **`/web-wlan`（监听 `0.0.0.0`）的安全与校验**：当前按约定先不做，凭证（URL 片段里的随机 token）是唯一凭据。待补：
  - 恢复 Host/Origin 白名单：只放行回环与本机网卡地址（防 DNS rebinding / CSRF），并决定是否允许用主机名访问。
  - 静态资源是否也要求凭证；局域网请求是否只放行 SSE 与 action。
  - 闲置回收策略（如 N 分钟无活动自动退回仅 loopback）与 TUI 风险提示的强度。
  - 局域网访问的审计与限流（记录来源 IP、限制失败尝试）。
